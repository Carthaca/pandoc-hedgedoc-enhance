# Pull Request for pandoc-ext/info

## Instructions

1. Fork https://github.com/pandoc-ext/info
2. Edit `README.md`
3. Find the appropriate section (likely "Highlighting and Annotations" or create "Text Processing")
4. Add this entry:
5. Create a pull request

## Entry to Add

```markdown
- [pandoc-hedgedoc-enhance](https://github.com/Carthaca/pandoc-hedgedoc-enhance) -
  HedgeDoc-style features for Pandoc: custom highlight colors with named colors
  (==text==) and user attribution labels ([name=Someone]). Zero configuration -
  automatically includes Font Awesome CSS and LaTeX packages. Email-friendly emoji mode.
```

## Alternative Detailed Entry (if more space is preferred)

```markdown
- [pandoc-hedgedoc-enhance](https://github.com/Carthaca/pandoc-hedgedoc-enhance) -
  Brings HedgeDoc collaboration features to Pandoc documents:
  * Custom highlight colors: `==highlighted text==` with 46 named colors or hex values
  * User attribution labels: `[name=Someone]` with Font Awesome icons or emoji
  * Zero configuration: automatically includes all required CSS/LaTeX packages
  * Email-friendly mode for HTML emails (no external CSS)
  * Text-only coloring mode (no background highlighting)
```

## PR Title

```
Add pandoc-hedgedoc-enhance filter
```

## PR Description

```markdown
## Add pandoc-hedgedoc-enhance filter

This PR adds a new Pandoc Lua filter that brings HedgeDoc-style features to Pandoc documents.

### Features
- Custom highlight colors (==text==) with 46 named colors
- User attribution labels ([name=Someone])
- Zero configuration - auto-includes Font Awesome CSS and LaTeX packages
- Email-friendly emoji mode
- Text-only coloring mode

### Repository
https://github.com/Carthaca/pandoc-hedgedoc-enhance

### License
Apache 2.0

### Inspiration
Inspired by HedgeDoc's collaborative markdown editor features.
```

## After Creating the PR

Monitor the PR for feedback from maintainers and respond promptly to any requests for changes.
